 {Gallery.map((item, index) => {
                return (
                    <div key={index}>
                        <Image src={item.imageUrl} alt={item.title} layout="fill" objectFit="fill"/>
                    </div>
                )
            })}